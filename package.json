{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "npx lerna clean",
    "build": "npx lerna exec -- npm run build"
  },
  "devDependencies": {
    "lerna": "^5.6.2"
  }
}
