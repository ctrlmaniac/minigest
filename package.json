{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "clients/*"
  ],
  "scripts": {
    "clean": "npx lerna clean",
    "collectclients": "bash collectclients.sh",
    "build": "npx lerna exec -- npm run build",
    "postbuild": "npm run collectclients && git add . && git commit -m \"collected clients\" && git push"
  },
  "devDependencies": {
    "lerna": "^5.6.2"
  }
}
